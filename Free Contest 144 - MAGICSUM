#include <iostream>
using namespace std;

int n, a, b;
long long sum = 0;

int main() {
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> a >> b;
        sum += (b - a + 1) * (a + b) / 2;
    }
    cout << sum << endl;
    return 0;
}
